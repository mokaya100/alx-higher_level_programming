#!/usr/bin/python3


def square_matrix_simple(matrix=[]):
        """
    wordA function that computes the square
    value of all integers of a matrix.
    """
 new -matrix = [ ]                                                                                                      for cool in  matrix :                                                                                                  result = list (map(lambdax :x**2,col))                                                                                 new -matrix. append (result)                                                                                           return new - matrix
